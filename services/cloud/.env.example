# Local Supabase credentials come from `pnpm --filter @nto/cloud status`.
# Only integration tests may use a service-role key; never put it in apps/web or Studio.
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=
